# 意思決定ログ（DECISIONS.md）

> フルのADR（Architecture Decision Record）運用は行わず、「何を・なぜ決めたか」だけを
> 時系列で1ファイルに残す軽量版です。新しい判断をしたら、末尾に追記していってください。
> フォーマット：日付 / 決定内容 / 背景・理由 / 今後見直す可能性のある条件

---

## 2026-08-22：SNS投稿の中継にBufferを採用

**決定**：X・Threads・Instagramへの投稿は、各SNSのAPIに直接繋ぐのではなく、Buffer経由にする。

**背景・理由**：3媒体それぞれの公式APIに個別対応すると開発・保守コストが高い。Bufferなら3媒体まとめて月2,000〜2,700円程度で、API仕様変更への追従もBuffer側に任せられる。

**見直す条件**：Buffer分の固定費が惜しくなった場合、または特定媒体（特にX）だけ直接投稿に切り替えるメリットが明確になった場合。

---

## 2026-08-22：画像・動画は「AI生成の背景」＋「プログラム側で正確に重ねるテキスト」のハイブリッド構成にする

**決定**：Nano Banana Proには背景ビジュアルだけを作らせ、見出しや数値などの重要な文字情報はPuppeteerで別途正確に重ねる。

**背景・理由**：AI画像生成モデルはビジュアルは高品質だが、日本語テキストの正確なレンダリングにはまだばらつきがある。誤字・崩れのリスクを避け、可読性と情報の正確性を優先した。

**見直す条件**：日本語テキストレンダリング精度が十分に向上したと判断できた場合。

---

## 2026-08-22：Kling AIは公式APIではなく、fal.ai等のアグリゲーター経由の利用を推奨

**決定**：Kling公式の複雑なJWT署名認証を自前実装するのではなく、Bearerトークン認証で使えるアグリゲーターの利用を基本方針とする。

**背景・理由**：個人運用でJWT署名の実装・鍵管理まで抱えるのは運用負荷に見合わない。アグリゲーターなら数行のコードで安定運用できる。

**見直す条件**：本格的に生成本数が増え、公式APIの方がコスト優位になった場合。

---

## 2026-08-22：動画化・ナレーションは7本中、重要度上位3本のみに限定

**決定**：画像カードは5枚すべて作るが、Kling AIによる動画化とElevenLabsによるナレーションは上位3本のみとする。

**背景・理由**：全5本を動画化すると月額コストが3,000〜4,500円に達し、費用対効果が見合わない。上位3本に絞ることで月2,000〜3,000円に圧縮しつつ、体感的な質の低下を抑える。

**見直す条件**：収益が安定し、追加コストを許容できるようになった場合は5本に戻す判断もあり得る。

---

## 2026-08-22：noteへの投稿は完全自動化せず、記事生成のみ自動化し貼り付け・公開は手動とする

**決定**：note公式に投稿APIが存在しないため、非公式APIによる完全自動化は行わない。記事ドラフトの生成までを自動化し、実際の貼り付け・価格設定・公開はユーザーの手動作業とする。

**背景・理由**：非公式APIは仕様変更で予告なく壊れるリスクがあり、noteアカウント停止等のリスクも否定できない。1日3分の手動作業で足りるため、リスクを取るメリットが薄い。

**見直す条件**：note公式が投稿APIを正式公開した場合。

---

## 2026-08-22：話題（トピック）を追加しやすい構造にリファクタリング

**決定**：ニュースの収集条件・カテゴリカラー・SNSキャプション方針などを `config/topics/<slug>.json` に切り出し、GitHub Actionsのマトリクス機能で話題ごとに並行実行できるようにする。

**背景・理由**：将来別の話題（別ジャンルのSNS運用）を追加する可能性があり、後から大改修するより最初から拡張しやすい構造にしておく方が長期的なコストが低い。

**見直す条件**：実際に複数話題を並行運用する必要が生じた場合、フェーズ15の手順に沿って話題を追加する。

---

## 2026-08-24：外部で並行開発中の「X自動投稿システム」との統合は、現時点では見送る

**決定**：Cursor/ChatGPTで別途開発中のX投稿自動化システム（GitHub Actions直接投稿・冪等性ledger・Human Approval Gate等を実装）とは、今は統合しない。7日間のProduction Observationが完了し正式にGO判定された後、X投稿ロジックのみを部分的に移植する方針とする。

**背景・理由**：もう一方のプロジェクトは「AI生成コンテンツの投稿はStage 2として別フェーズで正式承認が必要」と自ら定めており、今統合するとそのガバナンス原則に反する。また、7日間の本番観測期間中に外部要因を混ぜると、観測データの信頼性が損なわれる。

**見直す条件**：もう一方のProduction Observationが「A. GO」と判定された時点。

---

## 2026-08-24：killスイッチ・冪等性ledger・軽量意思決定ログの3点を導入

**決定**：もう一方のプロジェクトが持つ安全設計のうち、①即時停止スイッチ（GitHub Actions Variables の `KILL_SWITCH`）、②投稿の重複防止ledger（`ledger/<topic>.json`）、③この意思決定ログ、の3点だけを移植する。フェーズゲート審査や厳格なDoD運用など、重厚なガバナンスプロセス全体は導入しない。

**背景・理由**：①②はマネタイズ運用における実害（誤投稿・信頼失墜）に直結するリスク管理であり、低コストで導入できる。③は将来の引き継ぎ・自己参照のために有用。一方、重厚な審査プロセスは、このプロジェクトが持つ「素早く試して直す」という強み・スピード感を損なうため見送る。

**見直す条件**：運用規模が拡大し、複数人が関わるようになった場合は、より厳格なガバナンスの再導入を検討する。

---

## 2026-08-24：効果測定は「タグ埋め込みは完全自動」「数値集計は週次・手入力ベース」の折衷案にする

**決定**：GA4トラッキング・UTMパラメータの付与はすべて自動化する一方、Buffer/GA4/noteの実際のクリック数・売上を自動取得するAPI連携までは行わず、週次で手入力する軽量な仕組み（`metrics/<topic>/<週>.json`）にとどめる。

**背景・理由**：各サービスの分析APIをすべて自動連携すると、認証設定・保守対象が大きく増える。個人運用の規模では、週1回・数分の手入力で得られる効果に対してリターンが見合わない。まずは「計測できる状態」を整えることを優先した。

**見直す条件**：収益規模が拡大し、手入力の手間がボトルネックになった場合は、GA4データAPI等の本格連携を検討する。

---

## 2026-08-24：外部API（Kling・ElevenLabs）は失敗しても全体を止めない設計にする

**決定**：Kling AI・ElevenLabsの呼び出しにリトライ（2〜3回）を実装した上で、それでも失敗した場合はエラーで全体を止めず、動画は静止画スライドショーに、ナレーションは無音に、それぞれ自動でフォールバックする。

**背景・理由**：外部APIの一時的な不調で「その日は何も投稿されない」事態が、収益機会の損失として最も避けたいリスクだった。多少品質が落ちても「出す」ことを優先する設計にした。

**見直す条件**：フォールバック発生頻度が高く、品質低下が無視できないレベルになった場合は、Kling/ElevenLabsの信頼性そのものを見直す。

---

## 2026-08-24：X向けとInstagram/Threads向けで画像アスペクト比を分ける

**決定**：Instagram/Threads向けの縦長（1080×1350）に加え、X向けの横長（1200×675）画像を別途生成する。

**背景・理由**：同じ縦長画像をXにそのまま使うと、タイムライン上でトリミングされ見出しが見切れる可能性が高い。背景画像（Nano Banana Pro生成分）は共有し、テキストレイアウトだけ作り分けることでコストを抑えた。

**見直す条件**：実際のクリック率データが集まり、効果が確認できなかった場合は簡素化を検討する。

---

## 2026-08-25：Kling AI連携は、汎用REST実装ではなくfal.ai公式クライアント（@fal-ai/client）を採用

**決定**：当初は汎用的な「Bearerトークン＋独自エンドポイント」というコードで実装していたが、実際のfal.ai仕様（ファイルアップロード・キュー管理が専用形式）に合わないことが判明したため、fal.ai公式のnpmクライアントに置き換えた。

**背景・理由**：fal.aiは画像をURLとして渡す必要があり、かつ独自のアップロードAPIを持つ。これを自前のfetch実装で再現するより、公式クライアントに任せる方が正確かつ保守性が高い。

**見直す条件**：fal.ai以外のアグリゲーター（PiAPI等）に切り替える場合は、この部分を再度書き換える必要がある。

---

## 2026-08-25：note記事のみ「物語調の連載エッセイ」形式に変更（SNS・サイトは対象外）

**決定**：note販売用記事は、ニュースの要約ではなく、一人称視点の語り手による連載エッセイ形式にする。X/Threads/Instagram投稿と`docs`サイト記事は、引き続き簡潔な情報提供の文体を維持する。

**背景・理由**：note.comは情報の希少性ではなく「語り口・視点」に価値を感じて課金する文化のプラットフォームである。AIニュース自体は無料で読めるため、情報の網羅性では差別化できない。物語的な語り口は「読む体験」という、他の無料メディアが再現しにくい付加価値になる。ただし事実の脚色は厳禁とし、変えるのはあくまで「視点・語り口」のみとしている。

**見直す条件**：実際の販売データが集まり、通常の解説形式の方が売上が良いと分かった場合は元に戻す。

---

## 2026-08-25：note記事の有料部分末尾に「今日からできる、ひとつだけのアクション」を追加

**決定**：物語調エッセイの締めくくりとして、読者が実際に行動を起こせる提案を1つ、自然な文章の中に含める。強制的な箇条書き・宣伝口調にはしない。

**背景・理由**：「読んで終わり」ではなく「行動して終わり」にすることで、有料コンテンツとしての満足度・継続購読意欲を高める狙い。既存のアフィリエイト導線とも相性が良い。ただし、宣伝色が強くなりすぎると信頼を損なうため、提案内容は日によって柔軟に変え、押し付けがましくならないようトーンを管理する。

**見直す条件**：読者から「押し付けがましい」という反応が多い場合は、頻度を落とすか廃止を検討する。

---

## 2026-08-25：note記事の結びに「生活への橋渡し」と「明日への静かな期待」の2要素を追加

**決定**：まとめの後に、①その日の内容が読者自身の仕事・生活にポジティブに繋がる可能性を示す一文、②明日も変化が起きているはずだという押しつけがましくない期待、の2つを含む短い結びを追加する。不安・焦りを煽る表現は明示的に禁止する。

**背景・理由**：継続購読を高めるには「役に立った実感」と「続きへの期待」の両方が必要と判断した。AIニュースというジャンルは不安を煽りやすいため、あえてポジティブなトーンを厳守するようプロンプトで縛った。

**見直す条件**：毎回同じような結びで単調に感じられるようになった場合は、パターンを増やすことを検討する。
